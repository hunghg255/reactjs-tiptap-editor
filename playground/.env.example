# Prefer a backend proxy; VITE_ values are visible in the browser bundle.
VITE_AI_PROTOCOL=openai
VITE_AI_MODEL=your-model-id
VITE_AI_BASE_URL=/api/ai
# For local testing with your own key only:
VITE_AI_API_KEY=
