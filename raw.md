- Learn how to build our floating toolbar, somehow our implementation does not correctly stably layout the floating toolbar, we can learn from this package.
- Learn how to search and replace properly, should highlight the matches.
- Drag block to other places, somehow our implenentation such as when dragging image item of image gallery, when pasted into the editor such as below a paragraph will cause that paragraph broke into 2 paragraph which is unexpected. 
- Article codeblock node does not work as expected. Clicking enter within the codeblock and cause text break into a new article codeblock. I think we did not correctly handle that. We want correct interaction experience in a codeblock like node like our article codeblock.
- Learn how to support the drag handle

- Learn how to integrate mermaid

- AI features
